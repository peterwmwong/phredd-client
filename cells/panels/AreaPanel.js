$("<link href='http://fonts.googleapis.com/css?family=Brawler' rel='stylesheet' type='text/css'>").appendTo("head");
define(['GameState'], function(GameState) {
  return {
    render: function() {
      return "Phredd Web";
    }
  };
});